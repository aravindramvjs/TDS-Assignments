name: Basic Workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
      
    - name: 23f3004068@ds.study.iitm.ac.in
      run: echo "Hello, world!"
      
    - name: Run additional tests
      run: |
        echo "Running tests..."
        echo "Tests completed!"